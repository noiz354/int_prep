# Prometheus scrape config for the local provider stack.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: signalroom-api
    static_configs:
      - targets: ["host.docker.internal:8787"]

  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
