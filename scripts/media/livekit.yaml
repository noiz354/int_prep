# LiveKit self-hosted config (dev). Production: set proper keys via vault/env.
port: 7880
bind_addresses:
  - ""
rtc:
  tcp_port: 7881
  port_range_start: 7882
  port_range_end: 7888
  use_external_ip: false
keys:
  devkey: dev-secret-change-me
logging:
  level: info
