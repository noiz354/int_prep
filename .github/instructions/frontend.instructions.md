---
applyTo: "src/**/*.{js,jsx,css}"
---

Use the `react-experience`, `candidate-portal-accessibility`, and `performance-bundle` project skills where relevant. Preserve semantic HTML, explicit accessible labels, keyboard interaction, safe media permission flows, and responsive layouts. Do not add a UI library without a measured bundle and accessibility justification.
